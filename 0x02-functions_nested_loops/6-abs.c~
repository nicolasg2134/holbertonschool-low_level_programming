#include "holberton.h"

/**
 * _abs - Entry point
 * @x: wopa
 *
 * Description: Show a message blablabla
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}
