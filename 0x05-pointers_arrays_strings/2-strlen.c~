#include "holberton.h"

/**
 * _strlen - Swap the value of two integers
 * @s: Set of characters
 *
 * Return: i
 */

int _strlen(char *s)
{
	int i;

	while (*(s + i))
		{
	      i++;
		}
	 return (i);
}
