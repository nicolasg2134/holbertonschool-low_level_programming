#include "holberton.h"
#include <stdio.h>

/**
 * _abs - Return check the code for Holberton School students.
 * @n: Holds the integer for the function
 * Return: Always 0.
 */

int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
